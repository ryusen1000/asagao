<% @page_title = "ブログ記事の画像編集" %>
<h1><%= @page_title %></h1>
<h2><%= @entry.title %></h2>

<% url = entry_image_path(@entry, @image) %>
<%= form_with model: @image, url: url, local: true do |form| %>
  <%= render "form", form: form %>
  <div><%= form.submit %></div>
<% end %>
