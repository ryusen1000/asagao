<% @page_title = "ブログ記事への画像追加" %>
<h1><%= @page_title %></h1>
<h2><%= @entry.title %></h2>

<% url = entry_images_path(@entry) %>
<%= form_with model: @image, url: url, local: true do |form| %>
  <%= render "form", form: form %>
  <div><%= form.submit %></div>
<% end %>
